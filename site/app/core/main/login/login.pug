md-content.login-form(layout='row', layout-align='center center')
  md-card(flex, flex-gt-sm='50', flex-gt-md='33')
    md-toolbar
      .md-toolbar-tools(flex, layout='column', layout-align='center center')
        h2
          .icon.logo
        small SCANPAD
    md-card-content
      
      p(flex, layout='row', layout-align='center center')
      p Inicia sesión
      md-divider
      form(name='Form')
        md-input-container.md-block
          label Email
          input(ng-model='login.user.email', type='text', required)
          div(ng-messages='Form.email.$error', role='alert', multiple='')
            .my-message(ng-message='required') Ingresa tu email.
        md-input-container.md-block
          label Contraseña
          input(ng-model='login.user.password', type='password', required)
          div(ng-messages='Form.password.$error', role='alert', multiple='')
            .my-message(ng-message='required') Ingresa contraseña.
        md-button.md-raised.md-primary(ng-disabled='!Form.$valid', ng-click='login.login()', ng-if='!login.loading') Login
        md-progress-circular(md-mode='indeterminate', md-diameter='20', ng-if='login.loading')
        small.warning(ng-if='login.error')
          md-icon(md-colors='{color:"red"}') warning
          span(md-colors='{color:"red"}') {{login.error.message}}